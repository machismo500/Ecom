import { createRoot } from "react-dom/client";

import App from "./App";

const rootElement = document.getElementById("root");
const root = createRoot(rootElement);

root.render(<App />);

// Node package manager librarian who would give you these books
// Package.json - book card on which you would write that this package is what I am lookingf or
// node - library
// where a lot of javascript functions files would be kept

// npm install

// npm run start

// Only 1 html is what would go to the browser
// <App />  app()
